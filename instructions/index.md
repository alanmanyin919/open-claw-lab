# Instructions Index

Use this file as the entrypoint for navigating instruction content in this repository.

Navigation rules for Clawbot:
- Start at `instructions/index.md` when you need task-specific guidance.
- Choose the instruction file whose topic matches the current task.
- Once inside a task instruction file, treat that file as the single source of truth for configuration communication for that task.
- Do not search for alternate configuration guidance outside the referenced instruction file unless the file explicitly tells you to.

Available instruction content:

## Chrome Remote Desktop
- Path: `instructions/chrom-remote-desktop/install-chrome-remote-desktop.md`
- Use when the task is to install and verify Chrome Remote Desktop on the machine.
